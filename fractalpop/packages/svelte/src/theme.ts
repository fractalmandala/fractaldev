/** Theme → CSS custom properties (--fp-*), re-exported from fractalpop core. */
export {
  type Theme,
  type ThemePalette,
  type ThemeToken,
  paletteVars,
  themeStyle,
  themeCss,
  sveltekit,
  sveltekitLight,
  sveltekitDark,
  vscode,
  vscodeLight,
  vscodeDark,
  themes,
} from 'fractalpop'
