# Developer Snippets

1. Develop package inside frcked
Run `pnpm dev` inside fractalpop/packages/core. It runs bunchee --watch in the background and re-compiles dist/ automatically whenever you save a file.